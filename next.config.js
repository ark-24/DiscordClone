/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "uploadthing.com",
            "localhost",
            "utfs.io"
        ]
    }
}

module.exports = nextConfig
